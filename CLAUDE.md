# Tomogaude — Project Instructions

## Feature Branch Workflow

Whenever the user asks to add a new feature:

1. Create a new git branch named after the feature (e.g. `feature/add-shop`)
2. Make all changes on that branch
3. Commit and push the branch
4. Ask the user to create a pull request — do NOT create one automatically
